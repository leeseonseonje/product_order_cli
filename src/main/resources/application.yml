spring:
  profiles:
    active: application
  datasource:
    url: jdbc:h2:tcp://localhost/~/order_cli
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true